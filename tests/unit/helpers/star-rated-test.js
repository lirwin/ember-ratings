import { starRated } from 'ember-ratings/helpers/star-rated';
import { module, test } from 'qunit';

module('Unit | Helper | star rated');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = starRated([42]);
  assert.ok(result);
});
